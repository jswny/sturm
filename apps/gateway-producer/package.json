{
  "name": "gateway-producer",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "typecheck": "wrangler types && tsc --noEmit",
    "lint": "biome check ."
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
