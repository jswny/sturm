{
  "name": "gateway-consumer",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "typecheck": "wrangler types && tsc --noEmit",
    "lint": "biome check ."
  },
  "dependencies": {
    "channel-buffer": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
